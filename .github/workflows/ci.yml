name: CI

# Run on every push and pull request to main. This is the safety net that
# prevents broken code from landing — previously every workflow in this repo
# was `workflow_dispatch` (manual only), so nothing gated `main`.
#
# Scope (intentionally minimal for the first iteration):
#   - Rust: `cargo test` (must pass). `cargo clippy` runs but does NOT block
#     (-D warnings is too strict today: ~200 pre-existing lints across the
#     codebase; flipping them is a separate cleanup task).
#   - Frontend: `tsc --noEmit` on src/ only. `next lint` is skipped because
#     the project has no ESLint config yet.
#
# Adding stricter checks later (clippy -D warnings, eslint, vitest) is a
# matter of editing this file once the underlying debt is paid down.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs for the same ref when a new commit is pushed.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: Rust (test + clippy)
    runs-on: windows-latest
    defaults:
      run:
        working-directory: frontend/src-tauri
    env:
      # Skip FFmpeg download (unit tests don't exercise the runtime sidecar).
      MEETILY_SKIP_SIDECAR_VERIFY: "1"
      # Empty out bundle.externalBin so tauri_build doesn't try to verify the
      # llama-helper sidecar is present on disk (it has no download path).
      # ACL manifest generation still runs — generate_context!() needs it.
      TAURI_CONFIG: '{"bundle":{"externalBin":[]}}'
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      # Cache ~/.cargo AND target/ — without this, a fresh CI run recompiles
      # the whole dependency tree (~5+ min). With it, runs after the first are
      # typically under a minute when no Cargo.toml changed.
      - name: Cache cargo registry + build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: frontend/src-tauri

      - name: Build tests
        run: cargo test --lib --no-run

      - name: Run tests
        # --skip excludes hardware-dependent tests that segfault on a CI
        # runner with no audio subsystem (cpal/WASAPI init crashes with
        # STATUS_ACCESS_VIOLATION). These run fine locally:
        #   test_get_output_device          — needs an output device
        #   test_check_permissions          — WASAPI permission probe
        #   test_list_system_audio_devices  — enumerates real devices
        # Pattern is a regex matched against the full test path.
        run: cargo test --lib --no-fail-fast -- --skip audio::playback_monitor --skip audio::system_audio_commands

      # Non-blocking clippy: surfaces lints for visibility but does not fail
      # the job. Promote to `-D warnings` once the existing ~200 lints are fixed.
      - name: Clippy (non-blocking)
        run: cargo clippy --lib --all-targets
        continue-on-error: true

  frontend:
    name: Frontend (tsc + vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          # package.json lives in frontend/, not repo root — without this
          # the action fails with ENOENT looking for /repo/package.json.
          package_json_file: frontend/package.json
          # frontend/package.json has no "packageManager" field, so the
          # action can't auto-detect a version — pin one explicitly. Match
          # the version the lockfile was generated with (local dev uses 11);
          # older pnpm (e.g. 9) reads `pnpm.overrides` differently and fails
          # with ERR_PNPM_LOCKFILE_CONFIG_MISMATCH.
          version: 11

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          # pnpm 11 (pinned above) requires Node >= 22.13.
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install deps
        run: pnpm install --frozen-lockfile

      # Type-check src/ (production code) and tests separately. tsconfig.ci.json
      # excludes test files + vitest config; tsconfig.vitest.json includes only
      # test files with vitest globals available. Splitting keeps each surface
      # focused and avoids pulling test-only types into the build.
      - name: Type-check (src)
        run: npx tsc --noEmit -p tsconfig.ci.json

      - name: Type-check (tests)
        run: npx tsc --noEmit -p tsconfig.vitest.json

      # Vitest covers pure utility/lib functions and the useTemplates hook
      # (invoke mocked). jsdom env + matchMedia/ResizeObserver stubs come from
      # vitest.setup.ts. No Tauri backend is needed — every `invoke` is mocked.
      - name: Run vitest
        run: pnpm test
